# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GitList' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GitList
  pod 'SnapKit', '~> 4.0.0'
  pod 'Alamofire', '~> 4.7'
  pod 'SDWebImage', '~> 4.0'

  target 'GitListTests' do
    inherit! :search_paths
    pod 'Quick'
    pod 'Nimble'
    # Pods for testing
  end

  target 'GitListUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
